# Old Changelog Versions

This directory is reserved for changelog files intentionally archived during future version maintenance.

Keep `CHANGELOG.md` at the repository root as the active changelog. Do not move or copy it here until an old changelog is deliberately archived. Use an explicit version in each archived filename so its covered history is clear.
